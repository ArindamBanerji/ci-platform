# start_dev.ps1 — Start AGE after every reboot.
# Requires Start-AGE function from MicrosoftPowershell_profile.ps1
#
# Usage:
#   .\scripts\start_dev.ps1
#   OR from anywhere after profile loads: Start-AGE

Start-AGE
